class Entry {
  String id;
  String description;
  double rating;
  String solution;
  DateTime createdAt;

  Entry({
    required this.id,
    required this.description,
    required this.rating,
    required this.solution,
    required this.createdAt,
  });
}
